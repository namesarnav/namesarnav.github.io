# Base URL of the deployed "Ask Arnav" Cloudflare Worker (see worker/), e.g.
# https://arnav-ask-chat.<your-subdomain>.workers.dev
# Baked in at build time — required for the hero chat to work.
NEXT_PUBLIC_CHAT_API_URL=
